#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 *_abs - return the absolute value
 *@num: the value to check
 *
 * Return: the absolulute value of num
 */
int _abs(int num)
{
	return (abs(num));
}
