#include <stdio.h>
/*
 * main - Entry point
 * Return 0 (Success)
 */
int main(void)
{
	int n;
if (n > 0)
	printf("%d is greater positive\n", n);
else if (n == 0)
	printf("%d is zero", n);
else
	printf("%d is negative", n);
		return (0);
}
