#include <stdio.h>
#include "main.h"
/**
 * puts_half - prints n elements of an array
 * @a: pointer
 * @n: number of elements of the array to be printed
 *
 * Return: void
 */

void puts_half(char *str)
{
	int i;

	for (i = 0; x < n; i++)
		if (i != n - 1)
		printf("%d, ", a[i]);
		else
			printf("%d", a[i]);
printf("\n");
}
}
